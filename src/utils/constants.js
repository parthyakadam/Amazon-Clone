export const BASE_URL = ".."
// .. pointing to data folder present in folder structure

export const GB_CURRENCY = Intl.NumberFormat("en-GB", 
    {
        style: "currency",
        currency: "GBP",
    }
);